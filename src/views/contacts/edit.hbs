<div class="container p-4">
    <div class="row">
        <div class="col-md-4  mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/contacts/edit/{{contact.id}}" method="POST">
                        <div class="form-group mt-4">
                            <input type="text" value="{{contact.name}}" class="form-control" name="name_contact" placeholder="Name contact" autofocus>
                        </div>
                        <div class="form-group  mt-4">
                            <input type="tel" value="{{contact.number}}" class="form-control" name="phone_number"  placeholder="Phone number" maxlength="9"  required>
                        </div>
                        <div class="form-group  mt-4 d-grid gap-2 col-6 mx-auto">
                            <button class="btn btn-success">Save</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>